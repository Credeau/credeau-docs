# Deployment: Testing Environment

## Overview

The testing environment is used for internal testing and quality assurance.

## Setup Instructions

1. Clone the repository.
2. Install dependencies using `pip install -r requirements.txt`.
3. Configure environment variables as specified in `.env.testing`.
4. Run the application using `python manage.py runserver`.

## Notes

- Use mock services for external dependencies.
- Enable verbose logging for debugging.